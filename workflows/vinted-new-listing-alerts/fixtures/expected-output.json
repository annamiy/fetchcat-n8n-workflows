{
  "description": "Assertions for a search where only the first synthetic listing matches the configured brand and size.",
  "assertions": [
    "The default schedule is hourly and the default Actor result limit is 10.",
    "Configuration accepts 1 through 50 results and rejects values outside that range.",
    "The first successful run records current matching IDs without sending Telegram when sendFirstRunAlerts is false.",
    "Only unseen listings matching the configured price, brand, and size filters reach Telegram.",
    "Telegram messages contain human-readable listing details and direct links in groups of at most five.",
    "An immediate duplicate rerun creates no Telegram writes.",
    "A Telegram failure does not commit listing IDs to the delivery ledger.",
    "Changing search criteria creates an independent first-run baseline."
  ]
}
